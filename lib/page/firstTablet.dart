import 'package:flutter/material.dart';

class FirstTablet extends StatelessWidget {
  const FirstTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MOCKexam'),backgroundColor: Colors.orangeAccent,),
    );
  }
}